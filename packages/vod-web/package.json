{
  "name": "vod-web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "server": "node ./bin/www",
    "dev:client": "cd client && yarn start",
    "dev:server": "cross-env DEBUG=vod:server nodemon ./bin/www",
    "dev": "concurrently --kill-others-on-fail \"yarn dev:server\" \"yarn dev:client\"",
    "start": "cross-env-shell NODE_ENV=production \"cd client && yarn build && cd .. && yarn server\"",
    "heroku-postbuild": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run build",
    "postinstall": "cd client && yarn install"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "compression": "^1.7.3",
    "config": "^2.0.1",
    "cookie-parser": "~1.4.3",
    "debounce": "^1.2.0",
    "debug": "~2.6.9",
    "dotenv": "^6.0.0",
    "express": "~4.16.0",
    "morgan": "~1.9.0",
    "query-string": "^6.1.0",
    "react-router-scroll-4": "^1.0.0-beta.2",
    "redux-thunk": "^2.3.0",
    "vod-object-storage-client": "^0.0.0",
    "vod-redis-client": "^0.0.0"
  },
  "devDependencies": {
    "concurrently": "^3.6.1",
    "cross-env": "^5.2.0",
    "nodemon": "^1.18.3"
  }
}
